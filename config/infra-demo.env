# Environment configuration for the lightweight Python HTTP health service.
# This file is read by the systemd service (infra-demo.service) at startup.

# The port the health service will listen on
PORT=8080

# The path to the application log file (used by Systemd for redirection)
LOG_PATH=/var/log/infra-demo/app.log
